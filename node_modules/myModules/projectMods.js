var exports = module.exports = {};

// Get project
exports.getProject = function(req, query, callback) {
    var db = req.db;
    var collection = db.get('projects');
    collection.findOne(query,{},function(e,doc){
        callback(doc);
    });
}