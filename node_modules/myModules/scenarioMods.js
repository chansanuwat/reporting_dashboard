var exports = module.exports = {};

// Get single scenario
exports.getScenario = function(req, query, callback) {
    var db = req.db;
    var collection = db.get('scenarios');
    collection.findOne(query,{},function(e,doc){
        callback(doc);
    });
}